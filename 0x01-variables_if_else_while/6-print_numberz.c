#include <stdio.h>
/**
 * main - Entry point
 * Description: Program to print base 10 numbers between 0 & 9
 * Return: always 0 (success)
 */
int main(void)
{
	for (int n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
